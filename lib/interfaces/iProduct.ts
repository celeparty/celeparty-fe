export interface iUpdateProduct {
  title: string;
  rate: number;
  main_price: number;
  minimal_order: number;
  price_min: number;
  price_max: number;
  kabupaten: string;
  description: string;
}
